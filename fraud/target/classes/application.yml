server:
  port: 8083
spring:
  application:
    name: fraud
  datasource:
    password: password
    url: jdbc:postgresql://localhost:50/fraud
    username: kosmala
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true